apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: <NAMESPACE>
  labels:
    app: user-service
spec:
  replicas: 1  # Reducido a 1 para desarrollo y cuentas estudiante
  revisionHistoryLimit: 2  # Limita ReplicaSets antiguos para ahorrar memoria
  strategy:
    type: Recreate  # Evita problemas de recursos al actualizar en dev
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      tolerations:
      - key: environment
        operator: Equal
        value: production
        effect: NoSchedule
      - key: components.gke.io/gke-managed-components
        operator: Equal
        value: "true"
        effect: NoSchedule
      containers:
      - name: user-service
        image: <REGISTRY>/user-service:0.1.0
        imagePullPolicy: Always
        ports:
        - containerPort: 8700
          name: http
        envFrom:
        - configMapRef:
            name: user-service-config
        resources:
          requests:
            memory: "64Mi"   # Mínimo para ajustarse a recursos disponibles
            cpu: "25m"       # Mínimo para ajustarse a recursos disponibles
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /user-service/actuator/health
            port: 8700
          initialDelaySeconds: 120
          periodSeconds: 10
          timeoutSeconds: 5
        readinessProbe:
          httpGet:
            path: /user-service/actuator/health
            port: 8700
          initialDelaySeconds: 90
          periodSeconds: 5
          timeoutSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: <NAMESPACE>
  labels:
    app: user-service
spec:
  type: ClusterIP
  ports:
  - port: 8700
    targetPort: 8700
    protocol: TCP
    name: http
  selector:
    app: user-service

